export const lang = window.lang = new Map;

export const items = window.items = new Map;
export const requirements = window.requirements = new Map;
export const recipes = window.recipes = new Map;

recipes.typelist = ['food', 'others'];
